Start each reply with "Bn coae."
End each reply by running the command `git status`. Never forget to run this command as it is useful for tracking changes.
The environment is Windows and the shell is PowerShell.
Whenever the user says "this looks perfect", you can commit and then push to master on the remote.