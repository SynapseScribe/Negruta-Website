# Build Stage - Issue #15

## Implementation

Removed duplicate `"🐟",` and `"🐠",` entries from `COLLECTIBLE_TYPES` array in `js/game.js`.

## Commits

- Remove duplicate collectible entries from COLLECTIBLE_TYPES
