# Plan - Issue #10

## Changes
- Add `jumpCount = 0;` to `resetGame()` in `js/game.js:113` (after `velocityY = 0`)

## Impact
- Single line addition, no visual or behavioral changes beyond fixing the bug
- No mobile/desktop considerations (logic fix only)
